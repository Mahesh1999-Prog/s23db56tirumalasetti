extends layout

block content
  h1= title
  - let image_names = ["A1.jpg","B1.jpg","O1.jpg","M1.jpg","G1.jpg"]
   -for(let i=0; i<3; i++){
    -let random= Math.floor(Math.random()*5)
     img(src=`/images/${image_names[random]}` alt=`Image Name k${random}` height="300px" width="300px")
    -}